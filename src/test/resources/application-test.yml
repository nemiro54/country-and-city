spring:
  datasource:
    url: jdbc:h2:mem:country-service
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  flyway:
    enabled: true
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:country-service
    user: sa
    password: password
#    locations: classpath:db/migration,classpath:db/testdata
